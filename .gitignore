.env
.DS_Store
keys.txt.backup

# Ansible-generated secrets (belt-and-suspenders with ansible/.gitignore)
ansible/.k3s-token
ansible/.kubeconfig

# BMAD tooling + outputs — local-only, never commit/push
.claude/
_bmad/
_bmad-output/
.agents/
skills-lock.json

# built kernel artifacts — regenerable via tools/rpi-kernel-48bit/build-48bit-kernel.sh.
# a 31MB tarball in HEAD made every flux clone move 31MB and blew the 60s timeout.
tools/rpi-kernel-48bit/out/
