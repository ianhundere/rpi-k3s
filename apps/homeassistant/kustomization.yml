apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ha
resources:
  - ha.ns.yml
  - ha.pvc.yml
  - ha.deployment.yml
  - ha.service.yml
  - wyoming.yml
  # zwave + usb device plugin stay dormant — separate project
  # - generic-device-plugin.yaml
  # - zwave.deployment.yml
  # - zwave.service.yml
