apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: unifi
resources:
  - namespace.yml
  - secret.yml
  # mongo-init configmap removed: bootstrap-only, and it leaked ${MONGO_PASS}
  # in plaintext. re-bootstrap from an empty data dir = createUser by hand.
  - nginx-proxy-configmap.yml
  - deployment.yml
  - service.yml
  - pv-csi.yml
  - pvc.yml
  - gateway.yml
