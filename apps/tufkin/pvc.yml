apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tufkin-postgres-pvc
  namespace: tufkin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-rpik3s
  resources:
    requests:
      storage: 1Gi
