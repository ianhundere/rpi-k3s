apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: filebrowser
  name: filebrowser
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.ingress.kubernetes.io/limit-connections: "10"
    nginx.ingress.kubernetes.io/limit-rps: "2"
    nginx.ingress.kubernetes.io/proxy-buffers: "16 1024k"
    nginx.ingress.kubernetes.io/proxy-busy-buffers-size: "2048k"
    nginx.ingress.kubernetes.io/proxy-temp-file-write-size: "2048k"
spec:
  tls:
    - hosts:
        - "${FILEBROWSER_HOST}"
      secretName: filebrowser-cert
  rules:
    - host: "${FILEBROWSER_HOST}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: filebrowser
                port:
                  number: 44301
